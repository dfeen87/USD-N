{
  "name": "usd-n",
  "version": "0.1.0",
  "private": true,
  "description": "USD-N reference implementation (simulation + invariants).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "sim": "node dist/cli/sim.js",
    "dev": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
